문제
준식이는 수열 A[1], A[2], ..., A[N] (N ≥ 3) 을 가지고 있다.   1, 2는 반드시 가짐 
이 수열에서 단 한 개의 수를 제외하고 나머지 수는 모두 같다. 그 수는 수열에서 몇 번째에 위치할까?

if len(a) <3:
	continue

index +1 로 출력

input:
	t
	n (수열 길이)
	arr[]     if len(arr) <3:  break 
	
output:
	
	
